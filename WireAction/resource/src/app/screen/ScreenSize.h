#ifndef APP_SCREEN_SCREENSIZE_H_
#define APP_SCREEN_SCREENSIZE_H_

namespace app {
	static class ScreenSize {
	public:
		const static int width = 1920;
		const static int height = 1080;

		const static int widthHalf = width / 2;
		const static int heightHalf = height / 2;
	};
}

#endif
